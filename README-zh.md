# 彩虹图拼接工具

快速的将多张PNG无损拼合成彩虹图，用于**HOI4**的`frameAnimatedSpriteType` 。

## 使用方式

在PHOTOSHOP中制作带有时间轴的作品后，依次点击**文件** 、**导出** 、**渲染视频** ，**格式**选择为`PNG` ，**位数**选择为`5`  ，**Alpha通道**选择为`直接-无杂边`  ，确定**选择文件夹**输出位置为空文件夹，点击**渲染** 。打开**彩虹图拼接工具**，点击**选择文件夹**，选中为方才输出的文件夹，点击**开始拼接并保存**，输入文件名，即可成功导出。